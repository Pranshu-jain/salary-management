# Backend API URL — set this in Vercel dashboard under Environment Variables
# For local dev, the Rails backend runs on port 3000 by default
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# For production (Railway backend), replace with your Railway service URL:
# NEXT_PUBLIC_API_URL=https://YOUR-BACKEND.up.railway.app/api/v1
